# Profiling traces and benchmark result dumps (large binaries, regenerable)
results/
*.trace.json.gz
*.trace.json
*.zip

# Analysis output dirs (xlsx/csv/gz, regenerable from scripts)
B200_GLM5_FP8_Analysis/
MI355X_GLM5_FP8_Analysis/
GLM5_FP8_Analysis_test/
*.xlsx
*.pkl

# Benchmark CSV/JSON outputs
*.csv
answer_sglang.json
val_sglang.json
result.jsonl

# Caches and scratch
__pycache__/
*.pyc
tmp/
tmp2/
tmp_output_srt.txt

# Analysis output dirs for the GLM-5.3-Flash flow (xlsx/csv are already
# ignored above; the .txt diagnostics in them are regenerable too)
analysis_GLM5.2/
analysis_GLM5.3/

# Written at runtime by tools/kill_sglang.sh so no pkill pattern ever lands in
# a shell command line (see the comment in that script)
tools/.kill_sglang_inner.sh
